<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- The main content view -->

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <include
            android:id="@+id/error_bar"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            layout="@layout/error_bar_layout"
            android:visibility="gone" />

        <FrameLayout
            android:id="@+id/content_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/ads_container"
            android:layout_below="@+id/error_bar" />

        <LinearLayout
            android:id="@+id/ads_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="horizontal" />
    </RelativeLayout>

    <!-- The navigation drawer -->

    <fragment
        android:id="@+id/navigation_drawer"
        android:name="com.inrix.reference.trafficapp.fragments.NavigationDrawerFragment"
        android:layout_width="@dimen/drawer_max_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/drawer_background_color" />

</android.support.v4.widget.DrawerLayout>