<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.inrix.reference.trafficapp.locationpicker.LocationPickerActivity" >

    <include
        android:id="@+id/error_bar"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        layout="@layout/error_bar_layout"
        android:visibility="gone" />

    <RelativeLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/error_bar" >

        <FrameLayout
            android:id="@+id/map"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >
        </FrameLayout>

        <Button
            android:id="@+id/saveLocation"
            style="@style/IncidentsDetailsButtonBarButtonStyle"
            android:layout_width="@dimen/location_picker_save_button_width"
            android:layout_height="@dimen/location_picker_save_button_height"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="@dimen/location_picker_save_button_margin"
            android:text="@string/save_label" />

        <ProgressBar
            android:id="@+id/saveProgress"
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="gone" />
    </RelativeLayout>

</RelativeLayout>